export type SandboxDto = {
    sandbox_id: number,
    student_id: string,
    oracle_username: string,
    oracle_password: string,
    expire_at: string,
    is_active: boolean,
    created_date: string,
    modified_date: string
}